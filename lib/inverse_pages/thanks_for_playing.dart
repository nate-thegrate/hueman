import 'package:flutter/material.dart';

class ThanksForPlaying extends StatelessWidget {
  const ThanksForPlaying({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Thanks for playing!'),
      ),
    );
  }
}
